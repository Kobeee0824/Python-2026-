#unlike list, elements inside a tuple cannot be modified
#kapag list, square brackets. kapag tuples, parenthesis
coordinates = (4, 5)
coordinates[1] = 10
print(coordinates[1])#mag error since tuples cannot be modified

coordinates = [(4,5), (6, 7), (88, 34)]

